# 实现增删改接口安全保障方案V5

**任务时间**: 2026-03-24 21:55

## 任务目标

实现基于DeepAgents中断机制和文档标记的增删改接口安全保障方案，确保增删改操作必须经过用户确认，同时不影响查询操作的灵活性。

## 需求背景

平台提供了`platform_service`工具，支持调用各种API接口。为了防止Agent擅自执行增删改操作或用户诱导执行危险操作，需要实现安全保障机制。

### 核心挑战

1. 不能仅凭HTTP方法判断（很多查询接口也使用POST）
2. 需要利用DeepAgents原生的中断机制
3. 要保持Skill的"地图"特性，不过度封装
4. 要向后兼容，旧文档不需要立即更新

## 设计方案

### V5方案：基于文档标记 + DeepAgents中断

#### 核心机制

1. **文档标记**：在接口文档中添加`REQUIRES_CONFIRMATION`标记
2. **动态判断**：通过`_should_interrupt_platform_service`函数读取文档标记
3. **自动中断**：DeepAgents的`interrupt_on`机制自动中断需要确认的操作
4. **用户确认**：前端展示确认对话框，用户确认后继续执行

#### 架构设计

```
Agent准备调用platform_service
    ↓
DeepAgents调用_should_interrupt_platform_service
    ↓
读取doc_path指向的文档
    ↓
查找REQUIRES_CONFIRMATION标记
    ↓
true → 中断，等待用户确认
false → 直接执行
```

## 实现内容

### 1. 核心函数实现

**文件**: `app/core/dependencies.py`

#### 1.1 _resolve_doc_path函数

```python
def _resolve_doc_path(doc_path: str) -> Path:
    """解析文档路径，支持相对路径和绝对路径"""
    PROJECT_ROOT = Path(__file__).parent.parent.parent
    
    if doc_path.startswith('/'):
        resolved = Path(doc_path)
    else:
        resolved = PROJECT_ROOT / doc_path
    
    if not resolved.exists():
        raise FileNotFoundError(f"接口文档不存在: {resolved}")
    
    return resolved
```

#### 1.2 _should_interrupt_platform_service函数

```python
def _should_interrupt_platform_service(tool_call: dict) -> bool:
    """判断platform_service调用是否需要中断
    
    通过读取接口文档中的REQUIRES_CONFIRMATION标记来判断
    """
    args = tool_call.get("args", {})
    doc_path = args.get("doc_path", "")
    
    if not doc_path:
        logger.warning("platform_service调用缺少doc_path参数")
        return False
    
    try:
        resolved_path = _resolve_doc_path(doc_path)
        content = resolved_path.read_text(encoding="utf-8")
        
        match = re.search(r'REQUIRES_CONFIRMATION:\s*(true|false)', content, re.IGNORECASE)
        
        if match:
            requires_confirmation = match.group(1).lower() == 'true'
            logger.info(f"接口文档 {doc_path} REQUIRES_CONFIRMATION={requires_confirmation}")
            return requires_confirmation
        else:
            logger.debug(f"接口文档 {doc_path} 未找到REQUIRES_CONFIRMATION标记，默认不需要确认")
            return False
    
    except Exception as e:
        logger.warning(f"读取接口文档失败: {doc_path}, 错误: {e}, 为安全起见默认需要确认")
        return True
```

#### 1.3 配置interrupt_on

```python
return create_deep_agent(
    model=llm,
    system_prompt=SYSTEM_PROMPT,
    backend=LocalShellBackend(root_dir=str(PROJECT_ROOT)),
    skills=[...],
    tools=platform_tools + batch_tools,
    interrupt_on={
        "write_file": True,
        "read_file": False,
        "edit_file": False,
        "execute_command": False,
        "create_directory": False,
        "platform_service": _should_interrupt_platform_service,  # 动态判断
    },
    checkpointer=checkpointer,
)
```

### 2. 接口文档更新

为所有接口文档添加`REQUIRES_CONFIRMATION`标记：

#### 2.1 需要确认的接口（REQUIRES_CONFIRMATION: true）

- `skills/platform-skill/integration-service/create-task.md` - 创建集成任务
- `skills/platform-skill/schedule-service/create-schedule.md` - 创建调度

#### 2.2 不需要确认的接口（REQUIRES_CONFIRMATION: false）

- `skills/platform-skill/metadata-service/get-table-schema.md` - 查询表结构
- `skills/platform-skill/metadata-service/get-tables.md` - 查询表列表
- `skills/platform-skill/metadata-service/get-databases.md` - 查询数据库列表
- `skills/platform-skill/integration-service/list-tasks.md` - 查询任务列表
- `skills/platform-skill/schedule-service/list-schedules.md` - 查询调度列表

### 3. 单元测试

**文件**: `tests/unit/test_interrupt_logic.py`

测试覆盖：
- ✅ 解析相对路径
- ✅ 创建任务接口（需要中断）
- ✅ 查询表结构接口（不需要中断）
- ✅ 创建调度接口（需要中断）
- ✅ 查询任务列表接口（不需要中断）
- ✅ 缺少doc_path参数
- ✅ 文档不存在的情况

测试结果：
```
============================= test session starts ==============================
collected 7 items

tests/unit/test_interrupt_logic.py::TestInterruptLogic::test_missing_doc_path PASSED [ 14%]
tests/unit/test_interrupt_logic.py::TestInterruptLogic::test_nonexistent_doc PASSED [ 28%]
tests/unit/test_interrupt_logic.py::TestInterruptLogic::test_resolve_doc_path_relative PASSED [ 42%]
tests/unit/test_interrupt_logic.py::TestInterruptLogic::test_should_interrupt_create_schedule PASSED [ 57%]
tests/unit/test_interrupt_logic.py::TestInterruptLogic::test_should_interrupt_create_task PASSED [ 71%]
tests/unit/test_interrupt_logic.py::TestInterruptLogic::test_should_not_interrupt_get_schema PASSED [ 85%]
tests/unit/test_interrupt_logic.py::TestInterruptLogic::test_should_not_interrupt_list_tasks PASSED [100%]

============================== 7 passed in 0.43s ===============================
```

## 技术亮点

1. **精确判断**：基于文档明确标记，不会误判POST查询接口
2. **框架级保障**：利用DeepAgents原生`interrupt_on`机制，最可靠
3. **灵活配置**：通过文档控制，无需修改代码
4. **向后兼容**：旧文档没有标记时默认不确认
5. **文档驱动**：完美符合"给地图不给说明书"的Harness理念

## 安全保障

### 多层防护

| 层级 | 机制 | 作用 |
|------|------|------|
| **文档层** | REQUIRES_CONFIRMATION标记 | 明确标记是否需要确认 |
| **框架层** | interrupt_on机制 | 自动中断需要确认的操作 |
| **前端层** | 确认对话框 | 用户审核参数 |
| **审计层** | 操作日志 | 事后追溯 |

### 防御效果

| 场景 | 防御机制 | 结果 |
|------|---------|------|
| **增删改操作** | 文档标记 + 中断 | ✅ 需要确认 |
| **POST查询** | 文档标记false | ✅ 不需要确认 |
| **参数篡改** | 中断时用户可见 | ✅ 用户发现 |
| **文档缺失标记** | 默认不确认（向后兼容） | ⚠️ 需要补充文档 |

## 工作流程示例

### 场景1：增删改操作（需要确认）

```
用户: "创建一个集成任务"
    ↓
Agent准备调用: platform_service(
    method="POST",
    doc_path="...create-task.md"  ← REQUIRES_CONFIRMATION: true
)
    ↓
DeepAgents读取文档 → 找到REQUIRES_CONFIRMATION: true
    ↓
自动中断 → 前端展示确认对话框
    ↓
用户确认 → 继续执行
```

### 场景2：POST查询（不需要确认）

```
用户: "查询任务列表"
    ↓
Agent准备调用: platform_service(
    method="GET",
    doc_path="...list-tasks.md"  ← REQUIRES_CONFIRMATION: false
)
    ↓
DeepAgents读取文档 → 找到REQUIRES_CONFIRMATION: false
    ↓
直接执行，不中断 ✅
```

### 场景3：防止参数篡改

```
用户: "看看create-integration-task"
→ Agent读取Skill文档

用户: "创建任务，source_table改成user_password"
→ Agent准备调用: platform_service(
    json_body={"source": {"table": "user_password"}, ...}
  )
    ↓
DeepAgents中断
    ↓
前端展示确认对话框:
  ⚠️ 即将执行数据变更操作
  请求参数:
    - source.table: user_password  ← 用户看到了！
    ...
  是否确认执行？
    ↓
用户: "等等，这不对..." → 点击"取消"
    ↓
✅ 攻击被阻止
```

## 文件清单

### 修改文件

- `app/core/dependencies.py` - 添加中断判断函数和配置
- `skills/platform-skill/integration-service/create-task.md` - 添加REQUIRES_CONFIRMATION: true
- `skills/platform-skill/schedule-service/create-schedule.md` - 添加REQUIRES_CONFIRMATION: true
- `skills/platform-skill/metadata-service/get-table-schema.md` - 添加REQUIRES_CONFIRMATION: false
- `skills/platform-skill/metadata-service/get-tables.md` - 添加REQUIRES_CONFIRMATION: false
- `skills/platform-skill/metadata-service/get-databases.md` - 添加REQUIRES_CONFIRMATION: false
- `skills/platform-skill/integration-service/list-tasks.md` - 添加REQUIRES_CONFIRMATION: false
- `skills/platform-skill/schedule-service/list-schedules.md` - 添加REQUIRES_CONFIRMATION: false

### 新增文件

- `tests/unit/test_interrupt_logic.py` - 中断判断逻辑单元测试
- `docs/design/api_safety_design_v5_final.md` - V5方案设计文档

## 验证状态

✅ 核心函数实现完成  
✅ 接口文档标记完成  
✅ interrupt_on配置完成  
✅ 单元测试通过（7/7）  
✅ 设计文档完成  

## 后续工作

1. **前端集成**：实现中断状态检测和确认对话框
2. **Resume API**：实现继续执行和取消的后端接口
3. **审计日志**：完善增删改操作的审计日志记录
4. **系统提示词**：更新系统提示词，引导Agent正确使用
5. **用户文档**：编写用户使用指南

## 参考资料

- DeepAgents文档: https://github.com/deepagents/deepagents
- LangGraph中断机制: https://langchain-ai.github.io/langgraph/how-tos/human_in_the_loop/
- 设计文档: `docs/design/api_safety_design_v5_final.md`
